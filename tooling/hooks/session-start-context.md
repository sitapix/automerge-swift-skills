Automerge Swift Skills plugin is active.

When the user's code imports Automerge or asks about CRDTs, collaborative data, document sync, or fork/merge — use the automerge-swift skills **before** answering from training data. Automerge has non-obvious patterns that differ from typical Swift data handling.

Entry point: `/automerge-swift:ask` routes to the right specialist skill automatically.

Skills available: automerge-swift (router), automerge-swift-core, automerge-swift-codable, automerge-swift-text, automerge-swift-sync, automerge-swift-modeling, automerge-swift-diag, automerge-swift-ref.
